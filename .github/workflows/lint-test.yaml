name: Lint and Test

on:
  pull_request:
  push:
    branches:
      - main
    paths-ignore:
      - '.github/**'

concurrency:
  group: '${{ github.workflow }} @ ${{ github.event.pull_request.head.label || github.head_ref || github.ref }}'
  cancel-in-progress: true

jobs:
  lint:
    uses: exzeo-devops/action-workflows/.github/workflows/ansible-lint.yaml@df7b4a6520bf1392403eaa77e24bf803213eca3a # v1.0.23
    secrets: inherit

  tag:
    needs: [lint]
    if: ${{ github.ref == 'refs/heads/main' }}
    uses: exzeo-devops/action-workflows/.github/workflows/github-tag.yaml@73ca257449ebfbcf967334d0e002a67d840c2288 # v1.0.23
    secrets: inherit